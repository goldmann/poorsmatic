queues:
  /queues/urls:

topics:
  /topics/terms:

services:
  twitter-service:
    service: TwitterService
    config:
      credentials:
        consumer_key: 'Consumer key'
        consumer_secret: 'Consumer secret'
        access_token: 'Access token'
        access_secret: 'Access token secret'

messaging:
  /topics/terms: TermConsumer

  /queues/urls:
    UrlScrapper:
      concurrency: 4
